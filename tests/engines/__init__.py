"""Engine interface tests (L2 in test pyramid).

Tests for TTSEngine Protocol compliance and engine registry functionality.
These tests use mocks and don't require GPU or model weights.

Run like::

    pytest tests/engines/ -v
"""
