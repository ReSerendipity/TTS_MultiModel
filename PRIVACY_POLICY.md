# TTS MultiModel 隐私政策

**生效日期**：2026-09-10 ｜ **版本**：v1.1

TTS MultiModel 是一款自托管的语音合成平台（VoxCPM2 / IndexTTS2，含声音克隆与 LoRA 训练）。我们遵循"本地优先、最小收集"原则，本政策说明数据处理方式。

## 1. 本地数据（默认不上传）

| 数据类型 | 处理方式 |
|---|---|
| 输入文本 | 发送至您配置的本地推理引擎（默认本机运行），文本不出设备 |
| 上传音频（克隆/训练素材） | 仅存储于本地，用于本次合成/训练，不上传任何服务器 |
| 生成结果（音频） | 保存于本地输出目录，由您自行管理 |
| 历史记录（合成记录、音色库） | 存储于本地数据库，可随时清空 |
| 水印密钥、配置 | 存储于本机配置与密钥文件，不随仓库分发 |

## 1.1 生成音频水印（v1.1 补充）

- 所有通过本软件生成的音频默认嵌入**不可感知的内容来源水印**（16–20kHz 频域扩频信号），用于 AI 生成内容溯源与滥用追责；
- 水印密钥（`data/.watermark_key`，32 字节）**首次启动时在本机随机生成**，仅存于本机（权限 0600/ACL 收紧），**不随安装包或仓库分发**——官方无法跨实例验证产物，任何人也无法伪造官方可验证的水印；
- **企业统一部署场景**：管理员可通过环境变量 `TTS_WATERMARK_KEY`（64 位十六进制）为受管机器注入统一水印密钥，实现组织级溯源验证；注入后本机不再自动生成独立密钥；
- 水印嵌入失败时默认降级为"写溯源侧车元数据（`<输出>.provenance.json`）+ 输出原音频"并在本地审计日志留痕；管理员可将失败策略配置为 `block`（`config.yaml → watermark.failure_mode`），此时无法嵌入水印的产出会被阻断；
- 水印信号对人类听感不可感知，不改变音频内容本身，不包含任何个人信息或上传内容。

## 2. 推理引擎的数据流向

- **默认（本地推理）**：VoxCPM2 / IndexTTS2 在本机 GPU 运行，文本与音频均不出设备；
- **可选云端 API**：若您配置了云端模型服务，输入将发送至对应服务商，受其隐私政策约束——请勿提交敏感个人信息。

## 3. 声音克隆与训练的数据注意

声音克隆与 LoRA 训练会处理**人声样本**，属于敏感数据。请仅使用您本人或有合法授权的音频素材；处理过程默认在本地完成，不上传。

## 4. 我们不收集的内容

本服务**不收集**任何个人信息、使用埋点或遥测数据；不依赖第三方分析服务。

## 5. 数据删除

删除本地上传目录、输出目录、音色库与历史数据库即可清除全部数据。

## 6. 政策更新

本政策如有变更将在本页面同步更新并标注版本号。

---

*本政策为初稿参考，不构成正式法律意见。*
