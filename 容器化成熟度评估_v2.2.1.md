# TTS_MultiModel v2.2.1 云原生与容器化体系完整性评估报告

> 评估对象：v2.2.1（`pyproject.toml` / `config.yaml` 实测版本）
> 评估基准：12-Factor App 原则 + 不可变基础设施（Immutable Infrastructure）
> 评估性质：**现状审计（Maturity Audit）**，非实施方案。仅客观评分 + 指出差距。
> 证据来源：仓库实际文件（Dockerfile / docker-compose.yml / .dockerignore / .github/workflows/* / config.yaml / config.py / app_server.py / pyproject.toml / requirements.txt）及本地目录体积实测。

---

## 0. 执行摘要（Executive Summary）

| 维度 | 成熟度 | 一句话结论 |
|------|:----:|------|
| Docker 镜像优化 | ⚠️ 低（~4/10） | 多阶段构建 + 非 root 用户做得对，但 **26.33 GB 模型权重被烤进镜像层**（.dockerignore 未排除 `model/`） |
| Kubernetes 部署 | ❌ 缺失（0/10） | 全仓**无任何 K8s / Helm / HPA / ConfigMap 资产** |
| 配置外部化 | ⚠️ 弱（~4/10） | `config.yaml` 烤进镜像、`api_auth/host/模型路径` 不可 ENV 覆盖；Docker 设置的 `TTS_AUTO_LOAD_*` 实为死配置 |
| GPU 透传与调度 | ✅ 基本可用（~6/10） | compose `nvidia` reservation + `nvidia/cuda` 运行时可行，但无多卡/MIG/配额编排 |
| 自动扩缩容 | ❌ 缺失（0/10） | 单 Worker、GPU 串行、内存态，天然不可水平扩展 |
| 可观测性 | ❌ 近乎缺失（~1/10） | **无 `/metrics`、无 Prometheus/Grafana、日志为文本格式** |
| **12-Factor 综合** | ⚠️ **~4.0/12** | 第 I/II/VII 较好，III/IV/VI/VIII/IX/X 偏弱 |

**最关键的两个阻断性事实：**

1. **容器默认无法启动**：`Dockerfile` 的 `CMD` 以 `0.0.0.0:7869` 监听（`app_server.py:67`），而 `run_server()` 内置安全网（`app_server.py:811-822`）规定——非本地绑定且 `api_auth` 未启用即**拒绝启动**。实测 `config.yaml` **没有任何 `api_auth` 段**（默认 `enabled=False`），`docker-compose.yml` 也未设置 `api_auth`。因此 `docker compose up` 会立即因安全网拦截而退出。
2. **镜像体积灾难**：`.dockerignore` 未排除 `model/`（本地实测 **26.33 GB**）、`reference_repos/`（40 MB）、`.env`、`lora/`、`data/`、`dist/`、`htmlcov/`。`COPY . .`（`Dockerfile:51`）会把这些全部烤入镜像层，构建上下文与最终镜像极其臃肿，直接违背不可变基础设施。

---

## 1. 子体系逐项评估

### 1.1 Docker 镜像优化（体积 / 层数 / 安全性）

**已做对的（正向证据）：**
- 多阶段构建：`builder` 仅用于编译 wheel，`runtime` 只复制产物（`Dockerfile:6,29`）。
- 非 root 用户：`groupadd/useradd ttsuser` + `USER ttsuser`（`Dockerfile:37-38,55`）。
- 基础镜像**版本钉死**为 `nvidia/cuda:12.1.0-runtime-ubuntu22.04`（非 `latest`）。
- 存在 `HEALTHCHECK`（`Dockerfile:63-64`）。
- `apt` 清理 `rm -rf /var/lib/apt/lists/*`、pip `--no-cache-dir`。

**严重缺口：**
- **`model/` 未进 `.dockerignore`** → 26 GB 权重烤入镜像（致命）。
- `.dockerignore` 同时漏掉 `.env`（**密钥泄露风险**，仓库根确有 `.env`）、`reference_repos/`、`lora/`、`data/`、`dist/`、`htmlcov/`、`coverage.xml`。
- `runtime` 阶段 `COPY --from=builder /root/.local` + 又 `pip install --user /tmp/*.whl` + 再 `COPY . .`：**wheel 与源码被重复打包**，层数偏高；可只装 wheel 不二次 `COPY . .`（模板/静态已随 wheel 的 `package-data` 进入）。
- `builder` 用 `python3.10`（ubuntu jammy 默认），而 CI 测 3.12/3.13、AGENTS 标注 3.11+；构建产物与运行环境存在版本漂移。
- 无 `STOPSIGNAL`、无镜像 `LABEL`（OCP/OCI 元数据）、无 `@sha256` digest 钉死基础镜像。
- `requirements.txt` 中 `torch>=2.5.1` 走 PyPI 默认 CUDA wheel（约 2.5 GB+），叠加模型后镜像不可接受。

### 1.2 Kubernetes 部署配置

- 全仓搜索 `kind: Deployment/StatefulSet/Service/HPA/ConfigMap`、`apiVersion: apps/v1`、`Chart.yaml`、`helm`：**0 命中**。
- 结论：**完全没有 K8s 落地资产**。当前编排仅停留在单机 `docker-compose`。

### 1.3 配置外部化（config.yaml vs ENV）

- `config.py` 的 ENV 覆盖机制仅 `_load_dotenv()`（`.env` 特定键：`ENVIRONMENT/LOG_LEVEL/LOG_PATH/TTS_MODEL_MODE/...`）与 yaml `environment:` 段，且是 `os.environ.setdefault`（系统 ENV 优先）。
- **`server.host/port`、`api_auth`、`models.*.model_path`、`history.db_path` 均只读 `config.yaml`**，不读 `TTS_SERVER_HOST` 等通用 ENV（`config.py` 未出现 `getenv("TTS_SERVER...")`）。
- `config.yaml` 被 `COPY . .` 烤入镜像且 compose **未挂载**，改配置必须重建镜像 → 违反 XII 因子 III。
- **死配置**：`Dockerfile:60-61` 与 `docker-compose:13-15` 设置 `TTS_AUTO_LOAD_MODEL/ENGINE`，但 `config.py` 不消费这两个变量；实际自动加载由 `config.yaml server.auto_load_model:false` 与 lifespan 的 `auto_engine` 默认值驱动 → 镜像/编排意图与实际行为脱节。

### 1.4 GPU 资源透传与调度

- `docker-compose.yml:22-28`：`deploy.resources.reservations.devices` 走 `nvidia` driver、`count:1`、`capabilities:[gpu]`（compose v2 已支持）。
- 运行时镜像为 `nvidia/cuda:12.1.0-runtime`，满足 CUDA 库依赖。
- 缺口：仅单卡预留，**无多卡调度、无 MIG 切分、无 `nvidia.com/gpu` 配额约束文档、无 `shm_size`**（PyTorch DataLoader/多 worker 默认 64 MB `/dev/shm` 易 OOM）、无 `pids_limit`/ulimits。

### 1.5 自动扩缩容策略

- 无 HPA / KEDA / 任何副本与扩缩定义（见 1.2）。
- 架构上不可水平扩展：单 Worker、per-engine `asyncio.Semaphore` 容量 1（硬约束 #4）、模型驻留 GPU 显存、SQLite 本地库 → 副本间无法共享状态，扩缩无意义。

### 1.6 可观测性集成（Prometheus / Grafana）

- 全仓搜索 `prometheus/prometheus_client/opentelemetry/grafana//metrics`：**仅 htmx.min.js 与 training.py 误命中**，无真实埋点。
- 应用**无 `/metrics` 端点**；日志为 `logging` 文本格式（`config.yaml logging.format`），非结构化 JSON，不便 Loki/ELK 采集。
- `app_server.py` 有 `/api/health/ping`（liveness）与 `/api/health/ready`（readiness，含模型进度）——这是唯一可观测性亮点，但仅供探针，非指标。

---

## 2. 反模式识别（Anti-pattern）

| # | 反模式 | 是否存在 | 证据与判定 |
|---|------|:----:|------|
| 1 | 特权容器运行 | ✅ **未发现** | Dockerfile `USER ttsuser`；compose `security_opt: no-new-privileges:true`（`docker-compose:33-34`）；无 `privileged:true`。**此项合规** |
| 2 | 资源无 limits | ⚠️ **部分存在** | compose 已设 `memory:16G`/`cpus:4.0`（`docker-compose:29-31`），但**缺 `pids_limit`、`shm_size`、GPU 硬上限**；K8s 侧完全无 limits |
| 3 | Hard-coded paths in container | ⚠️ **部分存在** | `config.yaml` 内 `model/VoxCPM2`、`data/tts_history.db`、`logs/app.log`、`app/cert.pem` 均为相对路径，运行时经 `ROOT_DIR` 解析为容器绝对路径；路径可随挂载变化但**未参数化到 ENV**，且 `data/` 未挂载 → 状态写死在容器内 |
| 4 | No health checks | ✅ **未发现** | Dockerfile + compose 均配 `HEALTHCHECK`（liveness，指向 `/api/health/ping`）。**此项合规**；建议补充 readiness（`/api/health/ready`）区分探针 |
| 5 | Image tag 使用 latest | ⚠️ **分级判定** | Dockerfile 基础镜像已钉 `12.1.0`（非 latest，合规）；但**全仓无任何镜像构建/推送流水线**（见 2.7），即不存在不可变发布的 tag 策略（无 semantic / git-sha tag）→ 供应链不可复现 |
| 6 | No graceful shutdown | ⚠️ **部分存在** | `app_server.py:259` `lifespan` 有 shutdown 分支（卸载模型、关 DB、关任务队列）；但 `uvicorn.run(app, host, port)`（`app_server.py:846`）**未设 `timeout_graceful_shutdown`**，未显式处理 SIGTERM 长任务排空；无 preStop 钩子。GPU 生成可能超 30 s 默认窗口被强杀 |

### 2.7 补充：CI/CD 流水线中完全缺失容器环节

- `.github/workflows/` 共 10 个：ci / security / e2e / benchmark / docs-consistency / pages-deploy / release-please / stale / update-baselines / release.yml。
- 搜索 `docker|build-push|ghcr|image`：**0 命中**。即 **没有任何 workflow 构建、扫描、推送镜像**，也没有 Trivy 镜像扫描（仅有 Trivy FS 密钥扫描）。镜像从不被 CI 验证，与"Dev/Prod 同构"原则相悖。

---

## 3. 12-Factor App 逐条评分

评分尺度：✅ 达标(2) / ⚠️ 部分(1) / ❌ 缺失(0)。综合 12/24 ≈ **4.0 / 12**。

| # | 准则 | 评分 | 评估依据 |
|---|------|:--:|------|
| I | Codebase（一仓一应用，VCS） | ✅ | 单仓单应用，代码入 git；`model/` 26 GB 应由 git-lfs/外部存储，否则仓库膨胀（建议核查 `.gitignore`） |
| II | Dependencies（显式声明） | ✅ | `pyproject.toml` + `requirements.txt` + `requirements-lock.txt` 齐全；但**模型权重作为"隐式依赖"被烤进镜像**，违反声明式 |
| III | Config（配置与代码分离） | ⚠️ | `config.yaml` 烤进镜像、不可 ENV 全量覆盖、`api_auth/host` 不可外部化；仅少数键支持 `.env` |
| IV | Backing services（视作附加资源） | ⚠️ | 历史库为容器内本地 SQLite（`data/tts_history.db` 未挂载），模型本地存储——**有状态、未外部化为可挂载/网络资源** |
| V | Build/Release/Run（严格分离） | ⚠️ | 多阶段构建清晰；但**无 CI 构建镜像、无发布 tag、无 registry**，release 与 run 脱节 |
| VI | Processes（无状态进程） | ⚠️ | 模型驻留内存、生成信号量、SQLite —— **本质有状态**，不满足无状态进程 |
| VII | Port binding（自包含监听） | ✅ | uvicorn 自监听 + `EXPOSE 7869`，绑定由代码/CMD 控制 |
| VIII | Concurrency（水平扩展） | ❌ | 单 Worker、GPU 串行、无法多副本并发（见 1.5） |
| IX | Disposability（易丢弃/快启停） | ⚠️ | 模型加载慢、无 preStop 排空、`timeout_graceful_shutdown` 缺失；lifespan 清理存在但不够稳健 |
| X | Dev/prod parity（环境一致） | ⚠️ | 开发为 Windows 原生跑，生产为容器；CI **不构建/不运行容器**，镜像未被验证，环境漂移 |
| XI | Logs（事件流） | ⚠️ | uvicorn 输出 stdout（合规），但**额外写文件日志且为文本格式**，非纯 stdout 事件流、非 JSON |
| XII | Admin processes（管理任务） | ⚠️ | 模型下载（`install.sh`/`scripts/download_*.py`）、LoRA 训练（独立模块，硬约束 #3 不进启动路径）相对隔离；但无独立一次性任务（Job/CronJob）抽象 |

---

## 4. 改进建议（概要，按优先级）

> 本节仅给出方向性建议，供后续立项参考；非本次审计交付物。

**P0（阻断 / 致命）：**
1. **让容器能启动**：在 `config.yaml` 增加 `api_auth: {enabled: true, token: <from-env>}` 或改为监听 `127.0.0.1` + 反向代理；确保 `0.0.0.0` 场景下认证默认开启。
2. **从镜像剔除模型权重**：`.dockerignore` 增加 `model/`、`reference_repos/`、`.env`、`lora/`、`data/`、`dist/`、`htmlcov/`、`coverage.xml`；权重改为运行时挂载（compose 已挂 `./model`）。

**P1（成熟度关键）：**
3. 配置外部化：将 `host/port/api_auth/模型路径/DB 路径` 改为可读 ENV（如 `TTS_SERVER_HOST`、`TTS_API_AUTH_TOKEN`、`TTS_DB_PATH`），让 `config.yaml` 仅作默认值；compose 挂载 `./config.yaml` 或注入 ENV。
4. 建立镜像 CI：新增 `docker-build` / `docker-publish` workflow，按 `git sha` + semver 打 tag 推送 GHCR，并接 Trivy 镜像扫描；基础镜像加 `@sha256` digest。
5. 去除死配置：统一 `TTS_AUTO_LOAD_MODEL/ENGINE` 的语义或在文档中明确其无效。

**P2（健壮性 / 可观测）：**
6. compose 增加 `shm_size: "2gb"`、`pids_limit`、`cap_drop: [ALL]`、`read_only: true` + `tmpfs`；GPU 预留改为硬 `limits.devices`。
7. `uvicorn.run(..., timeout_graceful_shutdown=60)` + 显式 SIGTERM 排空；K8s 侧配 preStop 与 `terminationGracePeriodSeconds`。
8. 引入 `prometheus_client`，暴露 `/metrics`（请求数、队列深度、GPU 显存、生成耗时）；日志改 JSON 结构化。
9. 引入 K8s 资产（Deployment + StatefulSet/emptyDir 挂模型、ConfigMap 注配置、HPA 若未来支持多副本、PodDisruptionBudget）；或至少给出 Helm chart。

---

## 5. 证据索引（可追溯）

| 断言 | 文件:行 |
|------|------|
| 多阶段构建 | `Dockerfile:6,29` |
| 非 root 用户 | `Dockerfile:37-38,55` |
| 安全网拒绝 0.0.0.0 无认证启动 | `app_server.py:811-822,846` |
| config.yaml 无 api_auth 段 | `config.yaml` 全文（无 `api_auth`） |
| model/ 26.33 GB 且未排除 | 本地实测 + `.dockerignore`（无 `model/`） |
| `.dockerignore` 缺口 | `.dockerignore:1-15` |
| 死配置 TTS_AUTO_LOAD_* | `Dockerfile:60-61`、`docker-compose:13-15`、`config.py` 未消费 |
| liveness/readyness 端点 | `app_server.py:713,727` |
| compose GPU reservation + limits | `docker-compose:22-31` |
| 无镜像构建 workflow | `.github/workflows/*` 搜索 `docker` 0 命中 |
| 无 K8s/Helm 资产 | 全仓搜索 `kind: Deployment` 0 命中 |
| 无 prometheus 埋点 | 全仓搜索 `prometheus` 仅误命中 |
| config 不读通用 ENV | `config.py` 仅 `_load_dotenv` + yaml `environment:` |
